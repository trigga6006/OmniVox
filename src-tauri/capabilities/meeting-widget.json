{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "meeting-widget-window",
  "description": "Capability for the draggable Meeting Mode status widget",
  "windows": ["meeting-widget"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging"
  ]
}
